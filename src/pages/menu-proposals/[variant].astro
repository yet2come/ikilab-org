---
import Base from '../../layouts/Base.astro';
import Header from '../../components/Header.astro';
import { siteData } from '../../data/site';
import '../../styles/global.css';

const variants = {
  'slide-a': {
    title: 'Slide Compact (Base)',
    body: 'ベース案。JetBrains Mono + Zen Kaku Gothic New。モノスペース強調のコンパクト設計。',
  },
  'slide-a1': {
    title: 'Serif + Gothic',
    body: 'Instrument Serif（英語）+ Zen Kaku Gothic New（日本語）。クラシックで知的な組み合わせ。',
  },
  'slide-a2': {
    title: 'Noto Sans 統一',
    body: 'Noto Sans JP で英日統一。すっきり読みやすいモダンな印象。',
  },
  'slide-a3': {
    title: 'Gothic 太字',
    body: 'Zen Kaku Gothic New で英日統一。太めウェイトで力強い印象。',
  },
  'slide-a4': {
    title: 'Serif Italic',
    body: 'Instrument Serif イタリック（英語）+ Noto Sans JP（日本語）。エディトリアル風。',
  },
  'slide-a5': {
    title: 'Mono 全振り',
    body: 'JetBrains Mono で英日ともに表示。ターミナル的・テック感全開。',
  },
  'slide-a6': {
    title: 'Serif 大 + Gothic 小',
    body: 'Instrument Serif 大きめ + Zen Kaku Gothic New 小さめ。サイズコントラスト強め。',
  },
} as const;

export function getStaticPaths() {
  return ['slide-a', 'slide-a1', 'slide-a2', 'slide-a3', 'slide-a4', 'slide-a5', 'slide-a6'].map((variant) => ({
    params: { variant },
    props: { variant },
  }));
}

const { variant } = Astro.props as { variant: keyof typeof variants };
const current = variants[variant];
---
<Base title={`${current.title} — IKILAB`} description={current.body}>
  <Header
    brand={siteData.brand}
    brandHref="/"
    navItems={siteData.navItems}
    contactHref={siteData.contactHref}
    mobileMenuVariant={variant}
  />

  <main class="menu-proposal-preview">
    <section class="menu-proposal-hero">
      <div class="menu-proposal-eyebrow">Mobile Menu Preview</div>
      <h1>{current.title}</h1>
      <p>{current.body}</p>
      <div class="menu-proposal-links">
        <a href="/menu-proposals/" class="btn-brutal btn-ghost">一覧に戻る</a>
        <a href="/" class="btn-brutal btn-fill">本サイトへ →</a>
      </div>
    </section>
    <section class="menu-proposal-note">
      <p>このページはモバイル幅で開くと、ヘッダー右上のハンバーガーボタンからメニューを確認できます。</p>
    </section>
  </main>
</Base>

<style is:global>
  .menu-proposal-preview {
    min-height: 100vh;
    padding: 112px 20px 32px;
    background:
      radial-gradient(circle at top right, rgba(212, 43, 30, 0.10), transparent 30%),
      linear-gradient(180deg, #f8f6f1 0%, #fafafa 100%);
  }

  .menu-proposal-hero {
    max-width: 720px;
    margin: 0 auto 24px;
    padding: 28px;
    background: rgba(255,255,255,0.84);
    backdrop-filter: blur(12px);
    border: var(--border);
  }

  .menu-proposal-eyebrow {
    font-family: var(--font-mono);
    font-size: 10px;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--gray-500);
    margin-bottom: 10px;
  }

  .menu-proposal-hero h1 {
    font-family: var(--font-jp);
    font-size: clamp(36px, 8vw, 56px);
    line-height: 1.15;
    margin-bottom: 12px;
  }

  .menu-proposal-hero p {
    max-width: 560px;
    color: var(--gray-700);
    margin-bottom: 22px;
  }

  .menu-proposal-links {
    display: flex;
    flex-wrap: wrap;
    gap: 0;
  }

  .menu-proposal-note {
    max-width: 720px;
    margin: 0 auto;
    padding: 20px 28px;
    background: var(--white);
    border: var(--border);
    color: var(--gray-700);
  }
</style>
