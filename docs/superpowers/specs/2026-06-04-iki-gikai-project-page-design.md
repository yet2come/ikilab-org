# 設計: 「壱岐市議会みえる化」をプロジェクトとして掲載

- 日付: 2026-06-04
- 対象サイト: ikilab.org (Astro 静的サイト)
- 新規プロジェクト: iki-gikai (壱岐市議会 議事録 AI 構造化・検索サービス、本番稼働中 https://gikai.ikilab.org )

## 目的

本番稼働中の iki-gikai を、ikilab.org に**既存6プロジェクトと同じ見た目の詳細ページ**として掲載し、トップページの代表 Projects 枠にも載せる。

## 決定事項（ユーザー合意済み）

| 論点 | 決定 |
|---|---|
| 掲載先 | トップの代表6件にも掲載（6件入替） |
| 入替で外す枠 | **壱岐オープンダイアログ** を外し、削除せず `/projects` 一覧へ移設 |
| 詳細ページ方式 | **案A: DetailD コンポーネント方式**（`IkiCalendarDetailD` を雛形に複製） |
| サブスク記述 | **カット**（有料・会員の文脈を全削除） |
| 速報版議事録 | **機能としては残す**（開発中の機能として記載、whisper-1 採用は実績として残す） |
| 表示名 | **壱岐市議会みえる化** |
| 英タグライン | **Making the local council visible.** |
| URL | `/projects/iki-gikai`（slug は `iki-gikai`） |

## アーキテクチャ（変更ファイル4箇所）

ikilab.org のプロジェクト掲載は「Markdown 方式」と「DetailD コンポーネント方式」が併存するが、実際に使われているのは後者。1プロジェクト＝以下を触る。

1. **【新規】`src/components/projects/IkiGikaiDetailD.astro`**
   - `src/components/projects/IkiCalendarDetailD.astro` を雛形に複製。構造（7セクション・`pdd-*` クラス・`sections` ナビ・`whoLabels`）はそのまま、`d` オブジェクトの中身を iki-gikai に差し替える。スタイルは `global.css` の既存 `pdd-*` を共用（CSS 追加不要）。

2. **`src/pages/projects/[slug].astro`**
   - `import IkiGikaiDetailD from '../../components/projects/IkiGikaiDetailD.astro';` を追加
   - `getStaticPaths()` に `iki-gikai` エントリを追加
   - レンダリングの三項分岐に `entry.slug === 'iki-gikai' ? <IkiGikaiDetailD />` を追加

3. **`src/data/home.ts`**
   - `experiments.items` の6件目「壱岐オープンダイアログ」を **iki-gikai のカード**に差し替える（→ トップと `/projects` 一覧の両方に iki-gikai が出る。`[...slug].astro` が `home.ts` の items を一覧にも流し込むため）

4. **`src/pages/[...slug].astro`**
   - `additionalProjects[]` に「壱岐オープンダイアログ」を追加（`status: "active"`、`href` なし → 一覧では「詳細準備中」のカードとして残る）

## 詳細ページ コンテンツ（`IkiGikaiDetailD.astro` の `d` オブジェクト）

データソース: iki-gikai リポジトリの `docs/about-public.md` / `docs/roadmap-public.md` / `README.md`。

### Hero
- `title`: 壱岐市議会みえる化
- `status`: Operating
- `en`: Making the local council visible.
- `lead`: 壱岐市議会の本会議録を AI で構造化し、市民が検索・閲覧・俯瞰できるようにする非公式サービス。全文検索・AI 要約・議論テーマ抽出・可視化を提供する。
- `externalLabel`: gikai.ikilab.org を見る / `externalHref`: https://gikai.ikilab.org

### 01 背景（3カラム）
- どんな問題か: 壱岐市議会には独自の会議録検索システムがなく、本会議録は PDF として公開されるのみ。関心のある議題がどの会議で議論されたかを自力で当たる必要があり、横断検索や議員ごとの追跡が難しい。
- なぜ壱岐でやるのか: 規模が小さい議会だからこそ、全会議録の全量構造化が現実的に成り立つ。市民と議会の距離を縮める情報基盤になりうる。
- なぜ今やるのか: 埋め込み・要約などの AI が実用化し、市民有志でも議事録を構造化・検索可能な形に整えられるようになった。

### 02 実験内容（2カラム）
- 何をどう試しているか: 公開されている本会議録 PDF を取り込み、発言単位に構造化。キーワード×意味のハイブリッド検索、AI による会議概要・議員別 Q&A・論点の要約、複数 AI モデル統合による議論テーマ抽出までを一貫提供。2023〜2026年1月の 79 会議・約 6,000 発言・57 テーマを実データで運用中。
- 従来と何が違うか: 公式には検索系がなく PDF 閲覧のみ。本サービスは横断検索・議員ごとの追跡・テーマの年表化を可能にする。さらに4つの AI モデルを統合(ensemble)し、単一モデルの網羅率 35% 前後をテーマ網羅率 70% まで引き上げている。

### 03 STATUS（フェーズ ステッパー ＋ 現在の取り組み）
- phases: `取り込み・構造化` → `検索・AI要約` → `テーマ抽出・可視化` → `速報版議事録`
- currentPhaseKey: `速報版議事録`（最後）
- 現在の取り組み:
  - 公式音声 mp3 から AI 文字起こしで速報版議事録を生成（whisper-1 採用、CER 11.5%）
  - 速報版を検索結果・議員ページへ統合表示する設計
  - PDF 公開検知後に速報版を取り下げ、正本へ自動誘導する仕組み

### 04 知見
- 見えてきたこと:
  - 4つの AI モデルを統合することで、単一モデルでは取りこぼす論点まで拾え、テーマ網羅率が 35% 前後から 70% へ向上した。
  - 一般質問は本文中の議長の進行フレーズ（登壇促し・終了宣言）から、議員ごとのセッションに自動分割できた。
  - trigram 全文検索とベクトル検索を RRF で統合し、「キーワード一致」と「意味的な論点抽出」を1クエリで両立できた。
- 課題として残っていること:
  - 本会議録 PDF の公開は会議終了から 1〜3 か月後で、最新の議論を追うにはタイムラグがある。
  - 委員会記録は非公開のため、対象は本会議に限られる。
  - 情報収集・要約は AI API のレート制限とコストの制約を受ける。
- まだ答えの出ていない問い:
  - 速報版（音声由来の派生物）と正本 PDF の関係をどう設計し、誤読を防ぐか。
  - 市民有志による運営をどう持続させ、費用を負担するか。
  - 議会の関心テーマと市民の関心の乖離を、どう可視化して対話につなげるか。

### 05 NEXT
- 次にやること:
  - 速報版議事録を既存 UI（検索結果カード・議員ページ）へ統合表示
  - PDF 公開時に速報版を取り下げ、正本ページへ自動誘導
  - 文字起こし精度を PDF 公開後に再計算し、月次推移を可視化
  - アンケート機能と公開デモグラフィクスで、市民の関心テーマを集計・公開
- 再現性の見立て: PDF 取り込み→構造化→検索→テーマ抽出のパイプラインは、議事録を PDF 公開している他の地方議会にも汎用的に展開できる。技術より、誰が運営を担い継続するかという体制設計が再現の鍵。壱岐での実装知見を、地方議会の透明化テンプレートとして共有できる状態を目指す。

### 06 関わり方（4カード）
- 企業: 議事録オープンデータ化での連携（他自治体への横展開 / データ連携・API 設計 / AI 要約・検索基盤の共同開発）
- サポーター: データ補正・妥当性チェック（PDF 文字化けの補正 / テーマ分類のレビュー / 議員情報の確認）
- 研究者: 政治・行政テキストの分析（議論テーマの経年分析 / 議会と市民の関心の乖離研究 / マルチモデル抽出の評価）
- 個人: フィードバック・改善提案（使い勝手のフィードバック / 欲しい機能の提案 / 気づいた誤りの報告）

### 07 プロジェクト情報
- マイルストーン（バージョン履歴より、すべて 2026.05）:
  - 2026.05: プレビュー版(v0.1.1)公開。PDF 取り込み・全文検索・AI 要約・議員プロフィールを実装。
  - 2026.05: プライバシー方針・運営者情報を整備(v0.1.3)。
  - 2026.05: 複数 AI モデル統合による議論テーマ抽出とヒートマップ UI を公開(v0.2.0)。
  - 2026.05: 収録範囲を 2023〜2026 年の4年分(79会議・約6,000発言)に拡張(v0.2.1)。
  - 2026.05: 一般質問の詳細ダイアログ URL 共有に対応(v0.2.2)。
- 体制: （Members Only）— 企画・開発
- 関連プロジェクト: いきこども選挙（/cases/iki-kodomo-senkyo、市民参加つながり）
- 関連リンク:
  - 壱岐市議会みえる化（実サービス）: https://gikai.ikilab.org
  - 開発ロードマップ: https://gikai.ikilab.org/roadmap
  - このサービスについて: https://gikai.ikilab.org/about.html

### 最終 CTA
- 見出し: このプロジェクトに関心がある方へ
- 本文: 地方議会のオープンデータ化、議事録の構造化・検索、市民参加の仕組みづくりに一緒に取り組みたい方はご連絡ください。
- ボタン: お問い合わせ →（/contact）

## home.ts / 一覧カードのコピー

- トップ＆一覧カード（home.ts experiments.items）:
  - status: `active` / title: `壱岐市議会みえる化` / href: `/projects/iki-gikai`
  - body: 壱岐市議会の本会議録を AI で構造化し、全文検索・AI 要約・議論テーマ抽出で「議会で何が議論されているか」を市民が俯瞰できるようにする情報基盤。
- 一覧へ移すオープンダイアログ（[...slug].astro additionalProjects）:
  - status: `active` / title: `壱岐オープンダイアログ` / href なし
  - body: 既存 home.ts の文言を流用（事実に基づきながら、それぞれの主観に耳を傾ける「開かれた対話会」。対話の平等性、多様性の尊重、未来志向を大切にしています。）

## メモリ・ルールとの整合

「トップの Projects 枠は代表6件固定、新規は一覧のみ」のルールに対し、本件は**ユーザーの明示指示でトップ枠に掲載**（6件入替）。ルールの例外条件（明示指示時）に合致。

## テスト / 検証

- `npm run build` で全ページ生成・エラーなしを確認（現状 11 ページ → iki-gikai 追加で 12 ページ）。
- ローカル確認（`npm run dev`）で:
  - `/projects/iki-gikai` が既存詳細ページと同じレイアウトで表示される
  - トップページ Projects 枠に「壱岐市議会みえる化」が出る／「壱岐オープンダイアログ」が消える
  - `/projects` 一覧の Active に iki-gikai が出る／オープンダイアログが「詳細準備中」カードで残る
  - 外部リンク（gikai.ikilab.org 等）が新規タブで開く

## スコープ外（YAGNI）

- Markdown コンテンツ方式への移行や `[slug].astro` の分岐リファクタは行わない（既存パターン踏襲）。
- OGP 画像の個別作成は本件スコープ外。
- iki-gikai 側リポジトリの変更は行わない（参照のみ）。
